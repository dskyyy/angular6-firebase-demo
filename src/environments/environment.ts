// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyA0fOnSP4eGSs5lP4FnV5WxFUXe5G1arQg',
    authDomain: 'fir-angularapp-9c779.firebaseapp.com',
    databaseURL: 'https://fir-angularapp-9c779.firebaseio.com',
    projectId: 'fir-angularapp-9c779',
    storageBucket: 'fir-angularapp-9c779.appspot.com',
    messagingSenderId: '695998522180'
  }
}

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
